feat:
  datasource:
    driver: org.h2.Driver
    url: jdbc:h2:mem:feat;DB_CLOSE_DELAY=-1
    username: sa
    password:
  mybatis:
    path: mybatis/mybatis-config.xml
    initial-sql: mybatis/ddl/schema.sql
