package tech.smartboot.feat.cloud.aot.orm.script;

public enum TokenType {

    TEXT,

    IF_START,
    IF_END,

    WHERE_START,
    WHERE_END,

    TRIM_START,
    TRIM_END,

    FOREACH_START,
    FOREACH_END
}