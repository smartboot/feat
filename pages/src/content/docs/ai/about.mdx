---
title: Feat AI 简介
description: Feat AI 模块的设计理念与能力概览
sidebar:
    order: 0
---

import { Aside } from '@astrojs/starlight/components';

Feat AI 是 Feat 生态中的 AI 能力模块，为 Java 应用提供对话、嵌入、重排序、Agent 等能力，并与 Feat 的轻量、高性能风格保持一致。

## 背景介绍

在 Java 生态中集成大语言模型（LLM）时，开发者往往需要自行对接多种厂商 API、处理流式响应与工具调用。Feat AI 将常用能力抽象为统一 API，支持 Gitee AI、Ollama 等后端，便于在 Feat 项目内快速接入 AI 功能。

## 设计理念

- **统一入口**：通过 `FeatAI` 静态工厂创建各类模型（对话、嵌入、重排序、Agent），配置方式一致。
- **厂商无关**：通过 `ChatModelVendor`、`EmbeddingModelVendor` 等枚举或自定义 `baseUrl` 切换后端，代码侧无需大改。
- **与 Feat 一致**：基于 Feat 的 HTTP 客户端与异步风格，无额外重量级依赖，适合嵌入现有 Feat 服务。

## 能力概览

| 能力 | 入口方法 | 典型用途 |
|------|----------|----------|
| **对话** | `FeatAI.chatModel(...)` | 单轮/多轮对话、流式输出、函数调用 |
| **嵌入** | `FeatAI.embedding(...)` | 文本向量化，用于检索、RAG |
| **重排序** | `FeatAI.reranker(...)` | 对检索结果重排序，提升相关性 |
| **Agent** | `FeatAI.agent(...)` | ReAct 范式 Agent，结合推理与工具调用 |

<Aside type="tip">
**术语说明**：**ReAct**（Reasoning + Acting）是一种 Agent 范式：模型先推理任务，再调用工具执行，根据结果继续推理，直到完成任务。Feat AI 的 Agent 基于该范式实现。
</Aside>

## 架构要点

- **配置即代码**：`ChatOptions`、`EmbeddingOptions`、`AgentOptions` 等均采用链式配置，API Key 等支持环境变量（如 `FEAT_AI_API_KEY`）。
- **流式与同步并存**：对话支持 `chat()` 与 `chatStream()`，按场景选择。
- **可扩展**：Agent 通过 `AgentTool` 注册工具，Embedding 可与向量库（如 Chroma、Milvus）配合做 RAG。

## 扩展阅读

- [开始使用 Feat AI](/feat/ai/getstart/)：从零完成第一次对话。
- [对话与流式](/feat/ai/chat/)：配置对话模型与流式输出。
- [使用 Agent](/feat/ai/agent/)：使用 ReAct Agent 与工具。
- [Embedding 与向量](/feat/ai/embedding/)：文本向量化与典型用法。
