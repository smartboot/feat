---
title: 常见问题排查
description: 按现象而不是按模块组织的排错索引，帮助你更快定位 Feat 使用中的常见问题
sidebar:
  order: 1
---

这页不是“把所有知识点重新讲一遍”。  
它的目标更像一个排错索引：当你已经遇到某个现象时，先在这里判断应该去哪个方向查。

## 先按现象找方向

### 我连第一段响应都跑不出来

先看：

- [构建第一个 Feat HTTP 服务](/feat/server/getstart/)
- [从哪里开始读 Feat 文档](/feat/getting-started/)

常见原因：

- 依赖没加对
- 端口访问错了
- 程序启动后立即退出

### Feat Cloud 启动了，但控制器不生效

优先看：

- [构建第一个 Feat Cloud 应用](/feat/cloud/quickstart/)
- [Controller 开发实践](/feat/cloud/controller/)
- [CloudOptions 配置指南](/feat/cloud/options/)

最常见原因：

- 控制器没被扫描到
- 注解处理没有正确触发
- 路径理解错了，访问的不是最终映射 URL

### 能启动，但访问接口是 404

优先检查：

1. 路由路径是否与你访问的 URL 完全一致
2. 你用的是 `Controller` 路径还是 `Router` 路径
3. 修改代码后是否真的重新编译了

如果是 Feat Core 路由问题，看 [Router 路由组件](/feat/server/router/)。  
如果是 Feat Cloud 控制器问题，看 [Controller 开发实践](/feat/cloud/controller/)。

### 请求很慢，或者一个慢请求会拖住其他请求

优先看：

- [异步处理请求](/feat/server/async/)
- [ServerOptions 配置指南](/feat/server/serveroptions/)

先不要一上来就怀疑框架本身。  
很多时候只是你把耗时逻辑直接塞进了同步处理流程里。

### 想改端口、线程数、上传大小，但不知道该从哪开始

直接看 [ServerOptions 配置指南](/feat/server/serveroptions/)。  
这类问题不需要 FAQ 再重复展开。

### MyBatis 接进去了，但查不到数据

优先看：

- [在 Feat Cloud 中接入 MyBatis](/feat/cloud/db/)

重点排查：

1. `feat.yaml` 中的 `feat.mybatis.path` 是否写对
2. `initial-sql` 是否真正执行
3. mapper 包名是否与 `mybatis-config.xml` 一致

### AI 模型一直没有返回内容

优先看：

- [完成第一次 Feat AI 调用](/feat/ai/getstart/)
- [对话与流式](/feat/ai/chat/)

典型原因：

- 本地 Ollama 没起
- 模型名写错
- `FEAT_AI_API_KEY` 没进当前运行环境

### SSE 连上了，但就是收不到事件

优先看 [SSE 客户端教程](/feat/client/sse_client/)。

重点确认：

1. 响应头里的 `Content-Type` 是否真的是 `text/event-stream`
2. 你注册的是 `onData(...)` 还是命名事件 `onEvent(...)`
3. 服务端有没有真正持续输出事件

### WebSocket 连不上，或者连上后发消息报错

优先看 [连接第一个 WebSocket 服务](/feat/client/ws_client/)。

重点确认：

1. URL 是否用 `ws://` 或 `wss://`
2. 服务端是否真的支持 WebSocket 升级
3. 是否在 `onOpen(...)` 之前就发消息了

### HTTPS 跑不起来

优先看 [为 Feat 服务启用 HTTPS](/feat/server/https/)。

最常见原因：

- 证书路径不对
- PEM 文件没有打进资源目录
- 实际访问时仍然用的是 `http://`

### 应用能本地运行，但一进容器就挂

优先看 [交付一个 Feat Cloud 应用](/feat/cloud/deployment/)。

先查最朴素的问题：

1. Jar 是否真的打出来了
2. Dockerfile 的 `COPY` 路径是否正确
3. 启动主类是否写错

## 几个经常被问到的基础事实

### Feat 最低支持什么 JDK

JDK 8 或更高版本。

### Feat 的几个主要模块分别是什么

- `feat-core`：底层 HTTP 服务能力
- `feat-cloud` / `feat-cloud-starter`：更高层的注解式开发模型
- `feat-ai`：AI 能力接入

### 返回值什么时候会自动转 JSON

在 Feat Cloud 控制器里，`String` 会按文本返回；对象和集合通常会自动序列化为 JSON。  
具体表现可以直接看 [Controller 开发实践](/feat/cloud/controller/)。

## 如果你还是不确定该看哪一页

回到 [从哪里开始读 Feat 文档](/feat/getting-started/)。  
很多“排错”其实是因为一开始就走错了文档路径。
